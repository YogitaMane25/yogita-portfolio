import SectionTitle from '../common/SectionTitle';import SkillCard from '../skills/SkillCard';import {skills} from '../../data/data';export default function Skills(){return <section id="skills" className="section container"><SectionTitle eyebrow="02 / SKILLS" title="Tools I use to turn ideas into products."/><div className="skill-grid">{skills.map(s=><SkillCard key={s.title} skill={s}/>)}</div></section>}
