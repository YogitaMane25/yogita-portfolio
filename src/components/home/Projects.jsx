import {useState} from 'react';import SectionTitle from '../common/SectionTitle';import ProjectCard from '../projects/ProjectCard';import {projects} from '../../data/data';export default function Projects({full=false}){const [filter,setFilter]=useState('All');const [query,setQuery]=useState('');const shown=projects.filter(p=>(filter==='All'||p.category===filter)&&`${p.title} ${p.description} ${p.technologies.join(' ')}`.toLowerCase().includes(query.toLowerCase()));return <section id="projects" className="section container"><SectionTitle eyebrow="04 / PROJECTS" title="Selected work and practical solutions."/><div className="filters"><input placeholder="Search projects..." value={query} onChange={e=>setQuery(e.target.value)}/>{['All','Backend','Full Stack'].map(f=><button className={filter===f?'active':''} onClick={()=>setFilter(f)} key={f}>{f}</button>)}</div><div className="project-grid">{shown.map(p=><ProjectCard key={p.id} project={p}/>)}</div>{!full&&<a className="text-link" href="/projects">Explore all projects ↗</a>}</section>}
